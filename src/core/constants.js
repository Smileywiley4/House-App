/**
 * App-wide constants. Safe to use in React and React Native.
 * Brand colors: edit `src/design-tokens.js` (single source of truth).
 */
import { THEME as BRAND_THEME } from '../design-tokens.js';

export const APP_NAME = 'Propurty';

export const THEME = BRAND_THEME;
